import "package:culturapp/domain/models/actividad.dart";
import "package:flutter/material.dart";

class Filter {
  /*List<Actividad> applyFilter(List<Actividad> data, String searchText) {
    if (searchText.isEmpty) {
      return List.from(data);
    }
    return data.where((actividad) {
      return actividad.categoria?.toLowerCase().contains(searchText.toLowerCase()) ?? false;
    }).toList();
  }*/  

  //MODIFICAR PORQUE AHORA CATEGORIA ES UNA LIST.
}
